# 操作说明：每日自动更新（系统任务）

本文档说明如何在不常驻网页服务的情况下，让 RNTS 每天自动抓取并生成报告。
（如果你选择一直开着 `run.bat`，系统内置调度器会在每天 08:00 自动跑，无需看本说明。）

前提：已运行过 `setup_env.bat`（Windows）或 `bash setup_env.sh`（Linux/macOS）建好环境。

---

## 一、Windows：任务计划程序（推荐）

### 方法 A：一条命令注册（适合进阶用户 / Agent 部署）

在 `RNTS` 文件夹里**以当前用户或管理员打开 PowerShell**，运行：

```powershell
powershell -ExecutionPolicy Bypass -File install_daily_task.ps1
```

这会注册一个名为 **`RNTS_DailyFetch`** 的系统任务，每天 **08:00** 自动运行 `run_daily_task.bat`。
（脚本已自动加 `nopause` 参数，后台运行时不会卡住等输入。）

常用命令：

```powershell
Start-ScheduledTask  -TaskName "RNTS_DailyFetch"   # 立即手动跑一次测试
Get-ScheduledTask   -TaskName "RNTS_DailyFetch"   # 查看是否存在
Unregister-ScheduledTask -TaskName "RNTS_DailyFetch" -Confirm:$false   # 卸载
```

### 方法 B：图形界面手动建（小白友好）

1. 开始菜单搜「**任务计划程序**」并打开。
2. 右侧「**创建基本任务**」→ 名称填 `RNTS_DailyFetch`。
3. 触发器：**每天**，时间 `08:00`。
4. 操作：**启动程序**。
   - 程序或脚本：`cmd.exe`
   - 添加参数：`/c "C:\你的路径\RNTS\run_daily_task.bat" nopause`
   - 起始于（可选）：`C:\你的路径\RNTS`
5. 完成。右键该任务 →「**运行**」可立即测试。

> 提示：若勾选「不管用户是否登录都要运行」，任务会在后台静默执行（推荐），
> 但要确保运行账户对该 `RNTS` 目录有读写权限。

---

## 二、Linux / macOS：cron

先运行 `bash setup_env.sh` 建好环境，然后：

```bash
crontab -e
# 加入下面一行（每天 08:00，路径按实际修改）：
0 8 * * * cd /opt/rnts && /opt/rnts/.venv/bin/python -c "from app.database import init_db; from app.scheduler import run_daily_fetch; init_db(); run_daily_fetch()" >> /opt/rnts/data/daily_task.log 2>&1
```

---

## 三、怎么确认今天跑成功了？

打开 `data/daily_status.log`，应看到类似一行：

```
2026-09-02T08:00:12 status=success fetched=701 matched=103 new=30 purged=0 rc=0
```

字段含义：

| 字段 | 含义 |
|------|------|
| `status=success` / `rc=0` | 本次运行成功 |
| `fetched` | 本次抓取到的原始论文篇数 |
| `matched` | 按**当前配置**命中的篇数（报告据此生成） |
| `new` | 当天新增入库篇数（若当天已跑过，`new=0` 属正常） |
| `purged` | 本次清理（超过保留期的）篇数 |

- 若 `status=error`：打开 `data/daily_task.log` 看完整报错（通常是网络不通、或配置写错）。
- 想看抓取细节：看 `data/daily_task.log`（完整输出）。

---

## 四、可选：让网页服务也开机自启

如果你希望一直开着网页端（http://localhost:8000），让它在用户登录后后台常驻：

```powershell
powershell -ExecutionPolicy Bypass -File install_autostart.ps1
```

注册的任务名是 **`RNTS`**（注意和上面的 `RNTS_DailyFetch` 不同）：
- 它内置了每天 08:00 的抓取调度，**不需要再单独设每日任务**。
- 卸载：`Unregister-ScheduledTask -TaskName "RNTS" -Confirm:$false`

> 两种自启不要重复用：要么用 `RNTS`（网页常驻 + 内置调度），要么用 `RNTS_DailyFetch`（只跑每日抓取、不常驻网页）。
