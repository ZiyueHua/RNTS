# RNTS v1.1.3 更新说明（2026-09-17）

> 本次更新来自社区贡献（PR #2，作者 **funstonxu**）：**HTML 报告排版改进**，
> 外加三个实用的 Windows 辅助脚本 —— 重新生成报告、后台静默启动、一键停止。
> 同时修好了 Markdown 报告里论文信息被挤成一行的问题，并把新增脚本纳入了打包自检范围。

---

## 一、HTML 报告排版改进

| 项 | 之前 | 现在 |
|---|---|---|
| 正文宽度 | 固定 `max-width: 880px` | `width: min(92vw, 1600px)`，宽屏下铺得更开，窄屏仍自适应 |
| 论文信息 | 作者 / 日期 / 来源 / 关键词挤在同一行 | **四项各占一行**，层次清楚、便于扫读 |

Markdown 报告同步调整了信息分行（见第四节）。

---

## 二、三个新的 Windows 辅助脚本

三个脚本都只需**双击**，不需要命令行。

### 1. `generate_report.bat` —— 用已有数据库重新生成报告

**不重新抓取论文**，只按当前配置重新生成 `data/reports/` 下的报告。

适合两种场景：

- 你改了关键词 / 关注作者，想让历史报告立刻反映新配置；
- 你平时用 `run.bat`（常驻网页）而不用 `run_daily_task.bat`，想手动出一次报告。

> 它会先检查 `.venv\Scripts\python.exe` 是否存在，不存在会提示先跑 `setup_env.bat`。

### 2. `run_hidden.vbs` —— 后台静默启动

双击后在**后台启动** RNTS，不弹出控制台黑窗口。适合不想每次都看到命令行窗口的人。

> 停止它请用下面的 `stop.bat`，或者用任务管理器结束 python 进程。

### 3. `stop.bat` —— 一键停止

找出占用 **8000 端口**的进程并结束它，用于停止正在运行的 RNTS 服务。

> 它会结束该端口上的**任何**进程，不只是 RNTS —— 如果你机器上有别的程序也用 8000 端口，请注意。

---

## 三、修复与加固

| 问题 | 说明 |
|---|---|
| **Markdown 报告信息被挤成一行** | 作者 / 日期 / 来源 / 关键词四行的行尾补回了两个空格（Markdown 硬换行）。此前这四项在 md 报告里会被渲染成一段，反而比改之前更挤；HTML 报告不受影响。 |
| **新脚本未纳入自检** | `generate_report.bat`、`stop.bat` 已加入 `check_scripts.py` 的扫描清单，以后谁改坏它们的换行 / 编码，`python check_scripts.py` 会直接报出来。 |
| **`.vbs` 换行规则缺失** | `.gitattributes` 补上 `*.vbs text eol=crlf`。此前 `run_hidden.vbs` 会被兜底规则转成 LF，Windows Script Host 虽多能容忍，但不保险。 |

---

## 四、升级方式

升级前请先备份 `config/config.yaml` 和 `data/`。

**方式 A：只覆盖代码（推荐）**

解压本次分发包，把 `app/`、`templates/`、`static/` 和根目录脚本覆盖到你的 RNTS 目录，
**不要覆盖 `config/` 和 `data/`**。

**方式 B：git 用户**

```bash
git pull origin main
```

你的 `config/config.yaml` 与 `data/` 都在 `.gitignore` 里，不会被动到。

---

## 五、验证

升级后可以先跑一次自检，确认脚本没问题：

```bash
python check_scripts.py
```

应当输出 `共检查 23 个文件，0 个有问题。`（v1.1.2 时为 21 个）。

再双击 `generate_report.bat`，看能否正常生成报告；首页页脚显示的版本号应为 **v1.1.3**。

---

## 六、参与贡献

欢迎提交 Issue 与 Pull Request，动手前建议先读 [`CONTRIBUTING.md`](CONTRIBUTING.md) —— 里面写了本项目特有的几条红线（`.gitattributes` 不能删、`.bat` 必须纯 ASCII + CRLF 等），都是从实际踩坑里总结出来的。
