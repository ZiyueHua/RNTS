# RNTS v1.0.1 更新说明（2026-09-10）

> 本次更新主要解决两个反馈问题：**德语（变音字母）作者识别不了**、
> **在 config.yaml 里加的注释会消失**。同时把「自动重排配置」改成手动脚本。

---

## 一、修了什么

### 1. 德语 / 变音字母作者现在能正确识别

**之前**：匹配只按「原样字符串」比较，于是你在配置里写 `Jürgen Lisenfeld`，
而 arXiv 上的名字是 `Juergen Lisenfeld`，两者被视为不同的人，直接漏掉。

**现在**：同一个名字会生成多种等价写法再比对，以下情况都能命中：

| 情况 | 配置里写的 | 论文里的写法 | 结果 |
|------|-----------|-------------|------|
| 变音字母转写 | `Jürgen Lisenfeld` | `Juergen Lisenfeld` / `Jurgen Lisenfeld` | ✅ 命中 |
| 德语变音 | `Jörg Wrachtrup` | `Joerg Wrachtrup` | ✅ 命中 |
| Unicode 分解写法 | `Jürgen Lisenfeld` | `Jürgen Lisenfeld`（分解码位） | ✅ 命中 |
| 首字母缩写 | `R. J. Schoelkopf` | `RJ Schoelkopf` | ✅ 命中 |
| 姓/名顺序 | `Han Wang` | `Wang Han` | ✅ 命中 |

匹配仍是**整名比较**，`Han Wang` 不会误匹配 `Zihan Wang`，`Fei Yan` 不会误匹配 `Feifan Yan`。

### 2. config.yaml 里写的注释不会再丢

**之前**：系统用 `yaml.dump` 整体重写配置文件，注释、分组空行、甚至你手写的
RSS 源排列都会被抹掉；而每日任务又会自动重排一次，所以注释必然消失。

**现在**：
- 保存配置时**只重写** `keywords` / `highlight_authors_1` / `highlight_authors_2` 三个列表，
  其余内容（空行、缩进、引号风格、RSS 源写法）**一字不动**。
- 条目**上方**的注释行、条目**行尾**的 `# ...` 都会**跟着该条目走**，重排后依然在。
- 只有 `#` 前面是空白时才算注释，名字里紧挨着的 `#` 不会被误伤。

示例（这样写完全没问题）：

```yaml
highlight_authors_1:
# 德语区（变音字母 ü/ö/ä/ß 会自动兼容 Juergen / Joerg 等转写写法）
- Jürgen Lisenfeld  # Ulm
- Jörg Wrachtrup    # Stuttgart
- Alexandre Blais
```

### 3. 重排顺序改为手动（不再自动改写你的文件）

**之前**：每日任务会自动把三个列表按字母排序并写回，等于在你不知情时改动配置。

**现在**：每日抓取**只更新数据库和报告，完全不碰 config.yaml**
（实测跑完一次每日任务，配置文件 md5 不变）。需要整理时自己跑一次：

- **Windows**：双击 `sort_config.bat`
- **命令行**：`python sort_config.py`（加 `--check` 只预览会怎么变、不动文件）

`sort_config` 的行为：
- 写回前自动备份为 `config/config.yaml.bak`
- 排序为大小写、变音不敏感：`Jürgen` 按 `jurgen` 参与排序，不会掉到列表末尾
- 运行时会列出将要移动位置的条目，确认无误再写回

---

## 二、怎么升级

### 已经是老版本的用户（推荐）

1. 备份你现在的 `config/config.yaml`（**重要**：里面有你配的关键词和作者）。
2. 用新包里的这几个文件覆盖旧目录：
   - `app/` 整个目录（`config.py`、`filters.py`、`scheduler.py`、`main.py` 等）
   - `sort_config.py`、`sort_config.bat`（新增）
   - `README.md`（可选，文档）
3. **不需要重装 Python 依赖**，数据库 `data/rnts.db` 也不用动。
4. 想确认是否生效：跑一次 `sort_config.bat`，看它的提示里是否包含「注释已保留」。

### 全新部署

按 `README.md` 的「三分钟部署」走：双击 `setup_env.bat` 建环境，再双击 `run.bat` 启动。

---

## 三、其他调整

- `cloud_sync.dir` 不再内置任何固定路径（含个人用户名），默认留空、默认关闭。
  需要云同步时自己填目录，或用环境变量 `RNTS_CLOUD_SYNC_DIR` 设置。
- RSS 源地址统一为 `https`（arXiv / APS / Nature），避免部分网络拦截 http 跳转导致抓取失败。
- 所有 `.bat` 脚本均为**纯 ASCII + CRLF**，中文 Windows 的 cmd 下不会再出现脚本无法运行的问题；
  `.ps1` 为 UTF-8 BOM + CRLF。
- 统一了控制台编码（`.bat` 里 `chcp 65001` + `PYTHONIOENCODING=utf-8`）：
  Python 3.13 默认按 UTF-8 输出，而 cmd 默认是 936，两者不一致会让中文提示变成乱码。
  现在启动脚本、每日任务日志、排序脚本的中文都能正常显示。
- `sort_config.bat` 正确剥离自己的 `nopause` 参数，其余参数（如 `--check`）照常传给 Python。
- 新增 `check_scripts.py`：打包/发布前自检所有脚本的编码与换行（纯 ASCII+CRLF、BOM、LF）。

---

## 四、常见问题

**Q：我的注释是旧版本丢掉的，能恢复吗？**
A：如果没备份就恢复不了（旧版已覆盖写入）。`sort_config` 现在会在每次写回前备份为
`config/config.yaml.bak`，至少能回退一步。建议重要配置自己另外留一份。

**Q：为什么现在列表不再自动排序了？**
A：自动排序会在你没预期的时候改写文件，这正是注释丢失的根因。改成手动后，
配置文件只由你决定何时变动。

**Q：德语作者加了还是匹配不上？**
A：先确认名字拼写（姓 + 名的顺序），再确认论文里该作者确实出现在作者列表中；
可以临时把名字的 ASCII 转写形式（如 `Juergen Lisenfeld`）也加进列表做对照。
