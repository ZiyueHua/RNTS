# 操作指南：收藏与作者精选（v1.1.0 新页面）

> 启动方式不变：双击 `run.bat`，约 3 秒后浏览器自动打开 http://localhost:8000
> （也可以手动在浏览器输入这个地址）。

---

## 一、顶部导航

所有页面顶部都有同一条导航栏，当前页面会高亮，点一下即可跳转：

| 入口 | 页面 |
|------|------|
| 📋 论文列表 | 每日抓取的论文，默认只显示命中文献，可切换查看全部 |
| 👥 作者精选 | 近一年高亮作者的论文，按作者标签浏览 |
| ⭐ 我的收藏 | 你收藏的文献与分组管理 |
| 📊 统计 | 数量、趋势、排行 |
| ⚙️ 设置 | 关键词 / 高亮作者增删 |

右上角「⟳ 手动抓取」随时可手动抓一次今天的论文。

---

## 一·五、论文列表的显示范围（默认只显示命中文献）

论文列表页左侧筛选面板**最上方**有一个勾选框：

- **「只显示命中关键词 / 作者」——默认已勾选**，所以打开首页看到的是你真正关心的文献
  （命中关键词或高亮作者的），不再是库里的全部原始文献；
- **取消勾选**即切换为库中**全部原始文献**（含未命中的），方便你回看/复查是否有漏网之鱼；
- 列表上方的统计行会标明当前是「仅命中文献」还是「全部文献」，以及总篇数；
- 这个条件会**跟着翻页和导出**一起生效：翻页不会丢失该条件，
  右侧「报告导出」的 CSV / HTML / MD 也只导出当前视图的内容。

> 例：全部文献 1427 篇，勾选后只显示其中命中的 193 篇。
> 如果勾选后列表为空，说明当前配置下没有命中项——检查「设置」页的关键词 / 高亮作者，
> 或取消勾选先看全部。

---

## 二、收藏一篇文献

在以下三个地方的任意一处，点击 **☆ 星标** 即可收藏（再点一次取消）：

1. **论文列表页**：每张论文卡片右上角；
2. **论文详情页**：标题右侧；
3. **作者精选页**：每篇文献右端、日期旁边。

点击后星标立即变金色 ★（页面不刷新），这篇文献就进入「⭐ 我的收藏」了。

---

## 三、我的收藏页

导航点「⭐ 我的收藏」进入，页面分左右两块。

### 左侧：收藏夹（分组栏）

- **📚 全部收藏**：所有收藏，带总数；
- **📥 未分组**：还没归入任何分组的收藏；
- **🗂 你建的分组**：按分组筛选，带各自数量。

**新建分组**：在侧栏底部输入框填分组名（如「拓扑量子计算」「待精读」），点「添加」。

**重命名 / 删除分组**：先在左侧点进某个分组，右侧上方会出现
「重命名当前分组」「删除当前分组」按钮。
删除分组**不会删除收藏**，只是把组内文献变为未分组。

### 右侧：收藏卡片

每张收藏卡片从上到下：

1. 标题（点击进详情页）、来源标签、作者、日期；
2. **分组标签**（如 `🗂 拓扑量子计算`），标签上的 × 可把它移出该分组；
3. **备注**（如果写了）：📝 开头的灰底小段文字；
4. 底部一排操作按钮：

| 按钮 | 作用 |
|------|------|
| 📎 上传 PDF | 选择本机 PDF 上传，系统复制一份保存（推荐，最稳妥） |
| 🔗 挂本地路径 | 填本机 PDF 完整路径（如 `D:\papers\xxx.pdf`），不复制文件，仅在线预览 |
| 📄 xxx.pdf | 已有附件时显示，点击在新标签页预览；旁边标注是「上传副本」还是「本地路径」 |
| 移除附件 | 删掉 PDF 附件（上传的副本会一并删除） |
| 📝 备注 | 弹窗输入 / 修改备注（留空保存即清除） |
| + 调整分组 | 弹窗里勾选要加入的分组（可多选），保存即生效 |
| 取消收藏 | 移除这条收藏（有确认提示） |

顶部还有**搜索框**：按标题或备注关键词过滤当前收藏。

### 小贴士

- **上传副本 vs 本地路径怎么选？**
  上传副本随项目数据保存，换电脑时拷走 `data` 目录即可，不怕原文件移动；
  本地路径适合大文件不想复制的情况，但原文件被移动 / 删除后预览会提示失效。
- **收藏的文献不会被系统自动清理**，可以放心当长期文献库用；
  取消收藏后恢复普通的保留策略（未命中 31 天 / 命中作者 365 天）。
- 上传的 PDF 都存在 `data/attachments/` 目录下。

---

## 四、作者精选页

导航点「👥 作者精选」进入：

- 页面分 **🌍 国际团队** 和 **🇨🇳 国内团队** 两个区块；
- 每个区块里**每位作者一个标签**，标签上的数字是该作者近一年的论文数；
- **点击作者标签**，下方立即切换为该作者的论文列表（默认显示第一位有论文的作者）；
- 论文条目：标题可点进详情，右端有日期和**收藏星标**，可直接收藏；
- 页面顶部显示统计信息，并提供：
  - 「下载 HTML 报告」/「下载 Markdown」：近一年作者精选报告文件；
- 展示的是**当前配置**下高亮作者的命中结果——在设置页增删作者后这里立即更新。

---

## 五、常见问题

**Q：星标点了没反应？**
A：确认服务已启动（窗口没关）；刷新一次页面（Ctrl+F5）再试。

**Q：在哪里能看到我收藏了哪些？**
A：导航「⭐ 我的收藏」，或论文详情页底部「我的收藏」链接。

**Q：同一篇能进多个分组吗？**
A：能。点「+ 调整分组」勾选多个即可。

**Q：为什么作者精选页里有的作者标签不见了？**
A：只显示近一年**有命中论文**的作者；刚在设置里加了作者但还没有论文命中时不会出现。
