# RNTS v1.1.2 更新说明（2026-09-15）

> 本次更新**重做了 arXiv 源的抓取方式**：由「只走搜索 API」改为
> **RSS 公告层 + 时间窗 API 层**的双层结构。这解决了长期存在的
> 「arXiv 经常抓不到、每天最多只有 50 篇」的问题——**当搜索 API 被限流时，
> 公告层依然能拿到当天该分类的全部新论文**。
> 同时让抓取严格符合 arXiv 官方接口规范，并修好了升级后可能出现的重复条目。

---

## 一、为什么之前抓不到 arXiv

两层相互独立的障碍，之前只处理了第一层：

1. **网络层**：部分网络对 `arxiv.org` 域名的 TCP+TLS 握手做拦截（连接被立即重置）。
   程序已内置 **HTTP/3（QUIC）回退**，绕过它。
2. **arXiv 自身的限流**：官方对**搜索服务**按出口 IP 限流，表现为返回
   `429 Rate exceeded.`，或者干脆「拖延不响应」。搜索 API（`/api/query`、
   `/oai2`）和网站搜索共用这个后端，因此一旦被限流，它们全都不可用。

而 `rss.arxiv.org`（公告订阅源）、论文列表页、单篇 abs 页走的是另外的
（静态/缓存）路径，**不受这个限流影响**。所以新版把 RSS 公告源作为
**第一层**——它正是官方为「按分类获取新论文」提供的渠道。

---

## 二、arXiv 抓取：改为两层结构

| 层 | 接口 | 抓什么 | 说明 |
|----|------|--------|------|
| **第一层 公告层** | `https://rss.arxiv.org/rss/<分类>` | 当天该分类的全部新公告 | 一次请求（约 500KB），含**完整摘要与完整作者列表**；与搜索 API 的限流相互独立 |
| **第二层 时间窗层** | `https://export.arxiv.org/api/query` | 最近 `arxiv_lookback_days` 天（默认 3）提交的论文 | 按提交日期回溯，补齐某天没开机而漏抓的论文，并与公告层互相校验 |

两层结果**按论文链接自动去重合并**。公告层会跳过 `replace` / `replace-cross`
公告——那是已有论文的新版本公告，收下会把几个月前的老论文刷新成「本月新增」，
污染月报。

**效果**（实测同一天、同一网络）：

- 修改前：`[arxiv] 抓取失败`（被限流时整源为 0 篇），正常时受 `max_results=50` 截断
- 修改后：公告层拿到 **145 篇**当天新论文（含交叉列表），即使搜索 API 正被限流
- 每日任务的 arXiv 环节耗时从「被限流时 3–5 分钟」降到 **10 秒内**

---

## 三、严格按官方规范抓取

依据 arXiv 官方文档（Terms of Use 与 API 用户手册）实现：

- **限速**：所有 `*.arxiv.org` 请求串行执行、相邻请求间隔 ≥3 秒
  （官方要求「每 3 秒不超过 1 个请求、同一时刻只用一条连接」）；
  单页查询条数由 2000 调整为官方建议的 **1000**。
- **限流退避**：收到 429 时长时间退避并且**最多重试一次**——反复冲击只会刷新
  对方的限流窗口、让惩罚持续更久。
- **HTTP/3 时间预算**：对端「拖延不响应」时不再无限等待（每日任务 90 秒上限）。
- **按主机冷却**：某主机被限流后 90 分钟内不再请求它，直接用本地缓存；
  抓取成功自动解除。**公告层与搜索 API 分属不同主机，互不影响**。
- **手动补抓**（⟳ 补抓 arXiv）不受冷却期限制，但走「快速失败」模式：
  被限流时立即转用缓存，实测一次点击 **12 秒**返回（此前会卡 1–3 分钟）。

---

## 四、数据一致性修复

### 1. 链接规范化 + 升级自动迁移

搜索 API 返回的链接带版本号（`…/abs/2609.12345v1`），RSS 公告返回不带版本号的
写法（`…/abs/2609.12345`）。两种写法会让数据库按链接去重失效、同一篇论文重复入库。

现在统一为 `https://arxiv.org/abs/<id>`，并且**升级后首次启动（运行 `run.bat`
或每日任务）会自动把库里已有的 arXiv 链接规范化**——幂等、只改不删、无需手动操作。

### 2. 元数据「只增不减」

入库时不再盲目覆盖已有数据：

- `authors` / `summary` 取**更完整**的一方：APS 等源的 RSS 摘要是被截断的
  （末尾 `…`），补全后的完整摘要不会被后续抓取覆盖回截断版；
- 与作者相关的命中信息（`matched_authors` / `author_group`）只在新的作者列表
  至少同样完整时才更新；
- `date_added` 取**更早**的一方，避免同一篇论文的日期在提交日与公告日之间跳动。

### 3. 作者字段核对

公告层的作者是**完整**的（RSS 的 `<dc:creator>` 是逗号分隔的完整列表）。
已与论文 abs 页面逐条比对确认，无需担心作者匹配因数据源不同而漏判。

---

## 五、升级方法

### 已经是 v1.1.0 / v1.1.1 的用户

1. **备份 `config/config.yaml`**（里面有您的关键词与高亮作者）。
   > 仓库与发布包里只提供模板 `config/config.example.yaml`，`config.yaml` 属于你自己的配置、
   不在版本控制里；程序在 `config.yaml` 不存在时会自动复制一份模板。
   > **升级时只覆盖 `app/`、`templates/`、`static/` 与根目录脚本，跳过 `config/` 和 `data/`。**
2. 用新包覆盖：
   - `app/` 整个目录
   - `README.md`、`操作说明_每日任务.md`、`技术说明_arXiv抓取设计.md`（文档，可选）
3. **不需要动数据库**：升级后首次启动会自动把旧的 arXiv 链接规范化（幂等，可重复运行）。
4. **不需要装新依赖**：v1.1.0 起已包含所需的 `aioquic`（HTTP/3 回退）。
5. 验证：下一次每日任务日志里应出现
   `[arxiv] 公告层（RSS）抓取到 N 篇新论文` 与
   `[arxiv] arXiv 合计 N 篇（公告层 … + 时间窗层 …）`。

### 全新部署

按 `README.md` 的「三分钟部署」：双击 `setup_env.bat` 建环境，再双击 `run.bat` 启动。

---

## 六、可调项与已知限制

| 配置项 | 说明 |
|--------|------|
| `schedule.arxiv_lookback_days` | 时间窗层回溯天数，默认 `3`。**若某段时间没开机或 API 长期被限流，把它临时调大（如 `7`），下一次成功运行就会把漏掉的日期一并补回**；`0` 表示只取当天 |
| 源的 `rss_url`（可选） | 想指定别的分类/多个分类的公告源时，可在 `config.yaml` 的 arxiv 源下显式写 `rss_url:`；不写则由 `search_query` 里的 `cat:xxx` 自动推导 |

**已知限制**

- 出口 IP 被 arXiv 限流时（共享出口的宽带/移动网络较常见），时间窗层会进入
  90 分钟冷却并用缓存；此期间只有公告层提供当天新论文，标题/作者/摘要均完整。
- 历史日期补抓仍必须走搜索 API（RSS 公告只有当天、没有历史存档），
  因此被限流时补抓也只能返回缓存内容。缓解办法：稍后重试、临时调大回溯天数，
  或者重启光猫/路由器更换出口 IP（限流按 IP 计，通常可立即恢复）。
- 官方文档建议批量/集合收割使用 OAI-PMH，但它与搜索 API 同主机、共享同一限流，
  被限流时同样不可用，故未接入。

---

## 七、实测验证记录

| 项目 | 结果 |
|------|------|
| 当日公告层抓取 | 266 条公告 → 去重后 **145 篇**新论文（115 new + 30 cross） |
| 两层合并（含缓存兜底） | 195 篇，按链接去重后无重复 |
| 数据库完整性 | arxiv 记录零重复链接、零重复标题、零版本号残留 |
| 元数据 | 多作者论文作者列表完整（作者数分布 1–36 位），完整摘要未被截断版覆盖 |
| 手动补抓 | 12.4 秒返回（含公告层 + 缓存），重复补抓新增 0 篇（幂等） |
| 每日任务 | `status=success`、`rc=0`，报告正常更新 |

---

*更早的更新见 [《更新说明_v1.1.1.md》](更新说明_v1.1.1.md)、
[《更新说明_v1.1.0.md》](更新说明_v1.1.0.md) 与
[《更新说明_v1.0.1.md》](更新说明_v1.0.1.md)。*
