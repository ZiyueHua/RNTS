# RNTS v1.1.1 更新说明（2026-09-14）

> 本次更新集中在 **arXiv 抓取**：把「每次最多 50 篇」改成**按提交日期取全量**，
> 并新增**按日期补抓**功能（某天没开机也能补上）。
> 另外修正了抓取统计里「新增篇数」虚高、侧栏面板滚动时被导航栏遮挡两个问题。

---

## 一、arXiv 不再有 50 篇上限

**问题**：以前 arXiv 源的 URL 里写死了 `max_results=50`，而 quant-ph 每天新提交经常超过 50 篇，
多出来的会被静默丢掉——列表里看不到，报告里也没有。

**现在**：抓取时程序会在你配置的分类条件（`cat:quant-ph`）上自动追加提交日期区间，
并**自动翻页**把区间内的论文全部取回（单次请求上限 2000 条，超出继续翻页，页间间隔 3 秒）。

- **回溯窗口**：每日抓取取 `[今天 − N, 今天]` 提交的论文，N 由新配置项
  `schedule.arxiv_lookback_days` 控制，**默认 3 天**。
- **为什么要回溯，而不是只取当天**：arXiv 的官方公告比论文提交时间晚约 1–2 天，
  而且周一早上还要覆盖上周五与周末的提交。只取「当天」反而会漏。
- **重复抓取没有副作用**：重复条目由数据库按链接自动去重，所以窗口可以放心往前放宽。
- **其它 11 个 RSS 源（Nature / Science / APS / APL）完全不受影响**，行为与 v1.1.0 一致。

---

## 二、新功能：按日期补抓 arXiv

**场景**：某天没开电脑，那天的 arXiv 公告就错过了——以前没有办法补回来。

现在顶部导航栏右侧新增 **「⟳ 补抓 arXiv」** 按钮（在「⟳ 手动抓取」左边）：

1. 点一下按钮，展开一个小面板；
2. 面板里的日期**默认填昨天**，可以改成任意过去的日期（不允许选未来）；
3. 点「开始补抓」，完成后会提示新增了多少篇，并自动刷新页面。

补抓的具体行为：

- 抓取区间为 `[选定日期 − N, 选定日期]`（N 同上，默认 3 天）。因为公告滞后于提交，
  即便你按「刷到文章的那天」来选，往前回溯的窗口也能覆盖到。
- **只抓 arXiv 这一个源**，不动其它源。
- **只增不删**：补抓不会触发保留期清理，不会顺带删掉别的数据。
- **可以放心重复点**：同一天补抓多次也没关系，已入库的会自动跳过。

---

## 三、修正的问题

### 1. 抓取统计里「新增篇数」严重虚高

抓取完成后的提示、以及抓取日志里的 `papers_new`，以前实际统计的是「写入数据库的行数」——
SQLite 在 upsert 时会把**更新**也算进去。v1.1.0 之前每次抓到的几乎都是新条目，所以看起来正常；
但改成时间窗抓取后，每天都会重写窗口内的旧条目，这个数字就会虚高得很夸张
（比如实际只新增 3 篇，却显示新增 800 篇）。

现在改为按入库前后的总行数差来判断，显示的就是**真正新增**的篇数。

### 2. 侧栏面板滚动时被导航栏遮住

论文列表页左侧的筛选面板、收藏页左侧的分组面板都是吸顶（sticky）的，
但它们的吸附位置被硬编码成了 `67px`，而顶部导航栏的实际高度是 `102–106px`
（更窄的窗口下导航换行会变成 `164px`）。结果往下滚动时，面板顶部会被导航栏压住
（窄屏下最多压住 98px）。

现在改为**实测导航栏高度**再计算吸附位置，各种窗口宽度下都不会被遮挡。

### 3. 版本号统一（顺带）

程序版本号是 `1.1.0`，但**报告页脚和导出页脚一直写着 `RNTS v2.0`**（v1.1.0 更新说明里
提到过页脚不一致，当时只改了网页页脚）。

现在版本号收敛到唯一来源 `app/__init__.py`，程序启动日志、FastAPI 接口信息、网页页脚、
报告页脚、导出页脚全部读同一个值，以后不会再各写一个。

---

## 四、怎么升级

### 已经是 v1.1.0 的用户

**不用担心配置和数据**：本次没有新增依赖、没有改数据库表结构，`data/rnts.db` 不用动，
`config/config.yaml` 里的关键词和作者也不会受影响。

1. 备份你的 `config/config.yaml`（养成习惯，虽然本次不改它）。
2. 用新包覆盖以下内容即可：
   - `app/` 整个目录（`fetcher.py`、`scheduler.py`、`routes.py`、`config.py`、`main.py`、
     `reports.py`、`__init__.py` 均有改动）
   - `templates/base.html`、`templates/settings.html`
   - `static/css/custom.css`
   - `README.md` 与本文件 `更新说明_v1.1.1.md`（可选）
3. 重启 `run.bat` 即完成升级。顶部导航栏出现「⟳ 补抓 arXiv」按钮即为成功。

### `config/config.yaml` 要不要改？——**不改也完全能跑**

新逻辑兼容你原来的 arxiv 源 URL（里面的 `start` / `max_results` 会被程序覆盖），
`arxiv_lookback_days` 缺省时按 3 天处理。所以**升级不需要动配置**。

如果你想让配置文件和文档保持一致，可以手动改两处（也可以在「⚙️ 设置」页重新加载后确认）：

```yaml
- name: arxiv
  display_name: arXiv (quant-ph)
  url: https://export.arxiv.org/api/query?search_query=cat:quant-ph&sortBy=submittedDate&sortOrder=descending
  extractor: arxiv
  enabled: true
```

```yaml
schedule:
  daily_fetch_time: 08:00
  fetch_timeout: 30
  max_concurrent: 6
  per_page: 20
  arxiv_lookback_days: 3   # ← 新增；0 表示只取当天提交
```

> 只想抓当天的论文？把 `arxiv_lookback_days` 设为 `0` 即可。

### 全新部署

按 `README.md` 的「三分钟部署」走：双击 `setup_env.bat` 建环境，再双击 `run.bat` 启动。

---

## 五、常见问题

**Q：为什么抓回来的篇数比以前多了很多？**
A：以前被 50 篇上限截断的部分现在能取全了。窗口默认回溯 3 天，所以首次抓取会一次性补齐
这几天的论文；之后每天只增量新增。

**Q：我选了某个日期补抓，提示新增 0 篇，是坏了吗？**
A：不是。最常见的两种原因：① 那批论文之前已经抓过（去重后确实没有新的）；
② 你选的是周末或节假日，arXiv 当天没有公告。可以把日期往前调一两天再试——
这也是默认回溯 3 天的原因。

**Q：日报里「新增 N 篇」的数字可信了吗？**
A：可信了。以前那个数字把「更新已有条目」也算了进去，现在只统计真正的新条目。

**Q：补抓会不会把老数据删掉？**
A：不会。补抓只新增/刷新，不做任何清理。保留期清理仍然只在每日抓取时按原规则执行
（未命中 31 天、命中作者 365 天、收藏的永不清理）。

**Q：arXiv 又限流了怎么办？**
A：抓取失败时会自动重试，并在直连失败后走 HTTP/3 回退；如果全部失败且 48 小时内有成功缓存，
会先用缓存保证报告不断档。补抓功能可以随时手动再补一次。

---

*更早的更新见 [《更新说明_v1.1.0.md》](更新说明_v1.1.0.md) 与 [《更新说明_v1.0.1.md》](更新说明_v1.0.1.md)。*
