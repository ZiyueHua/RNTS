# RNTS v1.1.0 更新说明（2026-09-13）

> 本次更新围绕「日常用起来更顺手」：新增**文献收藏**、全新的**作者精选页**、
> 论文列表**默认只显示命中文献**、**导航整合与界面美化**，
> 并修复了一个可能导致 `run.bat` 无法启动的问题。

---

## 一、新功能

### 1. 文献收藏（分组 / 备注 / 本地 PDF 附件）

看到想长期保存的文献，点一下星标即可收藏，不用再翻列表找：

- **星标位置**：论文列表卡片右上角、论文详情页标题右侧、作者精选页每篇文献右端（日期旁），
  三处都可以直接点击收藏 / 取消收藏，页面不刷新、即时变色
  （☆ 未收藏 → ★ 已收藏金色）。
- **我的收藏页**：顶部导航新增「⭐ 我的收藏」入口。
  - 左侧收藏夹：全部收藏 / 未分组 / 你自建的分组，各带数量；点击切换筛选。
  - 右侧收藏卡片：标题、来源、作者、日期，以及备注、分组、PDF 附件信息与操作按钮。
- **分组**：一篇收藏可以**同时属于多个分组**；支持新建、重命名、删除
  （删除分组只解除归属，收藏本身保留）。
- **备注**：给收藏写一句话备忘（为什么收藏、重点看哪里），列表和详情页都会显示。
- **PDF 附件**（两种方式，可随时更换或移除）：
  - **上传副本**：把 PDF 上传，系统复制到 `data/attachments/` 随项目保存，不容易丢；
  - **挂本地路径**：只记录本机 PDF 的完整路径，不复制文件、不占空间，通过系统在线预览。
  - 两种方式都能点击在浏览器新标签页直接预览。
- **重要保护**：**被收藏的论文永远不会被自动清理**。原本系统会自动删除 31 天前的
  未命中论文，收藏过的内容现在不受保留期限制，可长期保存。

### 2. 作者精选页（导航新增「👥 作者精选」）

以前「作者精选」只是一份静态报告文件；现在是一个真正的网页：

- 取**近一年**内命中高亮作者的论文，分**国际团队 / 国内团队**两个区块；
- 每区块内**每位作者一个标签**（带论文数徽标），点击标签即切换到该作者的论文列表；
- 顶部提供近一年作者精选报告的 HTML / Markdown 下载入口；
- 与其他页面一样使用当前配置实时匹配——在设置里增删作者后立即生效。

### 3. 论文列表默认只显示命中文献

论文列表页（首页）以前默认把库中**全部原始文献**都列出来，看起来比较杂乱。现在：

- 筛选面板顶部新增 **「只显示命中关键词 / 作者」勾选框，默认勾选**——打开首页只看到你关心的文献；
- 取消勾选即可查看库中全部原始文献（含未命中的），两种视图随时切换；
- 该条件会**跟着翻页和导出**一起生效：翻页不丢条件，CSV / HTML / MD 导出也只导出当前视图的内容。

### 4. 导航整合与界面美化

- 顶部导航统一为：**📋 论文列表 / 👥 作者精选 / ⭐ 我的收藏 / 📊 统计 / ⚙️ 设置**，
  当前页面高亮，吸顶显示，各页面一键直达；
- 全站配色、卡片、标签、分页等样式统一精修（靛蓝紫主题），浅色 / 深色模式均适配；
- 论文列表侧栏底部的各类导出链接整理为「报告导出」分组区域。

### 5. 其他改进
- `run.bat` 启动后约 3 秒**自动用默认浏览器打开** http://localhost:8000 ，不再需要手动输入网址。
- 统一版本号为 v1.1.0（页面页脚此前与程序版本号不一致）。

---

## 二、修了什么

### run.bat 双击后窗口秒退

`run.bat` 的提示文字里含有英文括号，且正好位于 `if (...)` 括号块内部，
较新版本的 Windows cmd 会把它误解析为块结束符，导致启动即崩
（报 `. was unexpected at this time.`）。已修复。
`setup_env.bat`、`sort_config.bat`、`run_daily_task.bat` 排查过，无此问题。

---

## 三、怎么升级

### 已经是 v1.0.x 的老用户

1. 备份你的 `config/config.yaml`（里面有你的关键词和作者）。
2. 用新包覆盖旧目录（或只覆盖以下内容）：
   - `app/` 整个目录（新增 `routes_favorites.py`，改动了 `models.py`、`routes.py`、
     `scheduler.py`、`main.py`）
   - `templates/` 整个目录（新增 `favorites.html`、`authors.html`、`partials/fav_button.html`）
   - `static/css/custom.css`（样式全部更新）
   - `run.bat`（修复启动问题 + 自动打开浏览器）
   - `requirements.txt`（新增一项依赖）
   - `README.md` 及两份新文档（可选）
3. **需要装一个新依赖**（PDF 上传功能需要）：

   ```
   .venv\Scripts\pip install python-multipart
   ```

   或者干脆重装一遍：`.venv\Scripts\pip install -r requirements.txt`
4. 数据库 `data/rnts.db` **不用动**：收藏相关的三张新表会在下次启动时自动创建，
   已有论文数据不受影响。
5. 双击 `run.bat`，浏览器会自动打开；顶部导航出现「👥 作者精选」和「⭐ 我的收藏」即升级成功。

### 全新部署

按 `README.md` 的「三分钟部署」走：双击 `setup_env.bat` 建环境，再双击 `run.bat` 启动。

---

## 四、常见问题

**Q：升级后我的论文数据 / 配置会丢吗？**
A：不会。收藏是三张新表，自动创建；论文数据和 config.yaml 都保持原样。

**Q：收藏的论文还会被 31 天 / 365 天保留期删掉吗？**
A：不会。只要收藏着就永久保留；取消收藏后恢复普通保留策略。

**Q：挂了本地路径的 PDF 打不开？**
A：说明那个文件被移动、重命名或删除了。到「我的收藏」点「移除附件」后重新挂载即可
（上传副本方式没有这个问题）。

**Q：上传的 PDF 存在哪里？**
A：`data/attachments/` 目录。迁移电脑时把整个 `data` 目录一起拷走即可。

**Q：一篇文献能同时放进多个分组吗？**
A：可以。点收藏卡片上的「+ 调整分组」，在弹窗里勾选任意多个分组后保存。

---

*更早的更新见 [《更新说明_v1.0.1.md》](更新说明_v1.0.1.md)。*
